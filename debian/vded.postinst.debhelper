# Automatically added by dh_installinit
if [ -e "/etc/init/vded.conf" ]; then
	invoke-rc.d vded start || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
update-rc.d -f vded remove >/dev/null || exit $?
# End automatically added section
